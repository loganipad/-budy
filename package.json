{
  "name": "budy-study",
  "version": "1.0.0",
  "description": "",
  "main": "assets/layout/footer-loader.js",
  "directories": {
    "test": "tests"
  },
  "scripts": {
    "validate": "node scripts/validate-site.mjs",
    "build": "npm run validate",
    "test": "node --test tests/*.test.mjs",
    "build:mobile:web": "rm -rf mobile-web && mkdir -p mobile-web && rsync -a --delete --exclude '.git' --exclude 'node_modules' --exclude 'ios' --exclude 'android' --exclude 'mobile-web' --exclude '.venv' --exclude '.venv-1' --exclude 'tests' --exclude 'supabase' --exclude 'scripts' --exclude 'package-lock.json' --exclude 'package.json' --exclude '*.md' --exclude '.DS_Store' --exclude 'vercel.json' ./ mobile-web/",
    "mobile:sync": "npm run build:mobile:web && npx cap sync",
    "mobile:open:ios": "npx cap open ios",
    "mobile:open:android": "npx cap open android"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/loganipad/-budy.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "bugs": {
    "url": "https://github.com/loganipad/-budy/issues"
  },
  "homepage": "https://github.com/loganipad/-budy#readme",
  "dependencies": {
    "pdf-lib": "^1.17.1",
    "pdfkit": "^0.18.0"
  },
  "devDependencies": {
    "@capacitor/android": "^8.3.0",
    "@capacitor/cli": "^8.3.0",
    "@capacitor/core": "^8.3.0",
    "@capacitor/ios": "^8.3.0"
  }
}
